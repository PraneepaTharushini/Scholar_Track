{
  "name": "scholar-track-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix scholar-track-ui",
    "build": "npm run build --prefix scholar-track-ui",
    "preview": "npm run preview --prefix scholar-track-ui",
    "install:frontend": "npm install --prefix scholar-track-ui",
    "install:all": "npm install --prefix scholar-track-ui"
  }
}
